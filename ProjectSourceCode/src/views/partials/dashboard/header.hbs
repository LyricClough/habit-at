{{!-- src/views/partials/dashboard/header.hbs --}}
<div class="mb-6">
  <h1 class="text-3xl font-bold">
    Welcome back, {{#if user.username}}{{user.username}}{{else}}User{{/if}}!
  </h1>

  <div class="mt-2 flex items-center">
    <p id="date" class="text-gray-600"></p>

    <span class="ml-4 inline-block bg-indigo-100 text-indigo-800 px-2 py-1 rounded-full text-sm font-medium">
      {{#if streak}}{{streak}} Day Streak{{else}}Day Streak{{/if}}
    </span>
  </div>
</div>
